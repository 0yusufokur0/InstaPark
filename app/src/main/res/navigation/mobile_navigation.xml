<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/navigation_users">

    <fragment
        android:id="@+id/navigation_users"
        android:name="com.veripark.instapark.ui.main.users.UsersFragment"
        android:label="@string/users"
        tools:layout="@layout/fragment_users" />

    <fragment
        android:id="@+id/navigation_posts"
        android:name="com.veripark.instapark.ui.main.posts.PostsFragment"
        android:label="@string/posts"
        tools:layout="@layout/fragment_posts" />

    <fragment
        android:id="@+id/navigation_photos"
        android:name="com.veripark.instapark.ui.main.photos.PhotosFragment"
        android:label="@string/photos"
        tools:layout="@layout/fragment_photos" />
</navigation>