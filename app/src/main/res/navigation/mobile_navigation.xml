<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/navigation_home">

    <fragment
        android:id="@+id/navigation_home"
        android:name="com.veripark.instapark.ui.posts.PostsFragment"
        android:label="@string/users"
        tools:layout="@layout/fragment_posts" />

    <fragment
        android:id="@+id/navigation_dashboard"
        android:name="com.veripark.instapark.ui.users.UsersFragment"
        android:label="@string/posts"
        tools:layout="@layout/fragment_users" />

    <fragment
        android:id="@+id/navigation_notifications"
        android:name="com.veripark.instapark.ui.photos.PhotosFragment"
        android:label="@string/photos"
        tools:layout="@layout/fragment_photos" />
</navigation>